<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ADMIN</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi"
      crossorigin="anonymous"
    />
  </head>

  <body class="container" style="margin-top: 50px">
    <a href="/login" class="btn btn-info">ย้อนกลับ</a>
    <br /><br />
    <div class="card">
      <div class="card-header">
        <ul class="nav nav-pills w-100">
          <li class="nav-link pt-2"><h2>ผู้บริหาร บุคคลากร</h2></li>

          <li class="nav-link pt-2">
            <a href="/organize/add" class="nav-link active">เพิ่มข้อมูล</a>
          </li>
        </ul>
      </div>
      <div class="card-body">
        <% if(data_staffs.length) { %>
        <table class="table">
          <thead>
            <tr>
              <th scope="col">ลำดับ</th>
              <th scope="col">ชื่อ</th>
              <th scope="col">นามสกุล</th>
              <th scope="col">ตำแหน่ง</th>
              <th scope="col">เพิ่มเติม</th>

              <th width="col">แก้ไข / ลบ</th>
            </tr>
          </thead>
          <tbody>
            <% for(let i = 0; i < data_staffs.length; i++) { %>
            <tr>
              <th scope="row"><%= data_staffs[i].id %></th>

              <td><%= data_staffs[i].firstname %></td>
              <td><%= data_staffs[i].lastname %></td>
              <td><%= data_staffs[i].position %></td>
              <td><%= data_staffs[i].info %></td>
              <td>
                <a
                  href="../organize/edit/<%= data_staffs[i].id %>"
                  class="btn btn-success edit"
                  >Edit</a
                >
                <a
                  href="../organize/delete/<%= data_staffs[i].id %>"
                  onclick="return alert('Are you sure?')"
                  class="btn btn-danger delete"
                  >Delete</a
                >
              </td>
            </tr>
            <% } %>
          </tbody>
        </table>
        <% } %>

        <!-- if result is empty -->
        <% if(!data_staffs.length) { %>
        <p class="text-center">No book found!</p>
        <% } %>
      </div>
    </div>
  </body>
</html>
